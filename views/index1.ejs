<!DOCTYPE html>
<html>
	<head>
		<title>Bataille navale</title>
	</head>
	<body>
		<h1>Jeu de Bataille Navale</h1>
		<table>
			<% for(i=0; i < taille; i++){%>
			<tr>
				<% for(j=0; j < taille; j++){%>
					<td class="case adversaire" data-value="<%= String.fromCharCode(65 + i)%><%=j+1%>"><%= String.fromCharCode(65 + i)%><%=j+1%></td>
				<%}%>
			</tr>
			<%}%>
		</table>
		<br>
		<table>
			<% for(i=0; i < taille; i++){%>
			<tr>
				<% for(j=0; j < taille; j++){%>
					<td class="case joueur" data-value="<%=String.fromCharCode(65 + i)%><%=j+1%>"><%=String.fromCharCode(65 + i)%><%=j+1%></td>
				<%}%>
			</tr>
			<%}%>
		</table>

		<input type="text" id="change_nom" placeholder="Choisir un nom">
		<script src="/socket.io/socket.io.js"></script>
		<script src="js/jquery.js"></script>
		<script src="js/script.js"></script>
	</body>
</html>